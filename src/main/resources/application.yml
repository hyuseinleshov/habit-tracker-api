spring:
 application:
  name: habit-tracker-api
 datasource:
  url: jdbc:postgresql://localhost:5432/${POSTGRES_DB}
  password: ${POSTGRES_PASSWORD}
  username: ${POSTGRES_USER}
  driver-class-name: org.postgresql.Driver
 jpa:
  open-in-view: false
  show-sql: true
  hibernate:
   ddl-auto: update
  properties:
   hibernate:
    format_sql: true

jwt:
 secret: ${JWT_SECRET}
 issuer: ${JWT_ISSUER}
 expiration-duration: ${JWT_EXPIRATION_DURATION}
 not-before-leeway-duration: ${JWT_NOT_BEFORE_LEEWAY_DURATION}
 clock-skew-seconds: ${JWT_CLOCK_SKEW_SECONDS}





